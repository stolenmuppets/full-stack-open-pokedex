name: Hello World!

on:
  workflow_dispatch:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "Hello World!"
      - name: print current directory
        run: ls -l
      - name: print date
        run: date
